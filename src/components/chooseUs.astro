---

---

<div class="choose-us w-full min-h-screen py-16 px-4">
    <div class="flex flex-col items-center text-center mb-12">
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-800">
            Why Choose Us?
        </h1>
        <div class="line mt-3 w-24 md:w-32 h-1 bg-blue-600 rounded-full"></div>
    </div>

    <div
        class="w-full max-w-7xl mx-auto flex flex-col lg:flex-row justify-between gap-8"
    >
        <!-- Left Column -->
        <div class="w-full lg:w-1/3 flex flex-col gap-6 pb-0 lg:pb-4 p-4">
            <div
                class="bg-transparent transition-all duration-300 flex gap-4 rounded-xl p-5"
            >
                <img
                    class="w-12 h-12 object-contain"
                    src="/teeth-braces.webp"
                    alt="Orthodontics icon"
                />
                <div class="flex flex-col">
                    <h2 class="text-lg md:text-xl font-semibold text-gray-800">
                        Orthodontics
                    </h2>
                    <p class="font-light text-gray-600 mt-1">
                        Some big paragraph will go here so it is a dummy data
                        for now
                    </p>
                </div>
            </div>

            <div
                class="bg-transparent transition-all duration-300 flex gap-4 rounded-xl p-5"
            >
                <img
                    class="w-12 h-12 object-contain"
                    src="/teeth-braces.webp"
                    alt="Orthodontics icon"
                />
                <div class="flex flex-col">
                    <h2 class="text-lg md:text-xl font-semibold text-gray-800">
                        Orthodontics
                    </h2>
                    <p class="font-light text-gray-600 mt-1">
                        Some big paragraph will go here so it is a dummy data
                        for now
                    </p>
                </div>
            </div>

            <div
                class="bg-transparent transition-all duration-300 flex gap-4 rounded-xl p-5"
            >
                <img
                    class="w-12 h-12 object-contain"
                    src="/teeth-braces.webp"
                    alt="Orthodontics icon"
                />
                <div class="flex flex-col">
                    <h2 class="text-lg md:text-xl font-semibold text-gray-800">
                        Orthodontics
                    </h2>
                    <p class="font-light text-gray-600 mt-1">
                        Some big paragraph will go here so it is a dummy data
                        for now
                    </p>
                </div>
            </div>
        </div>

        <!-- Middle Column (Image) -->
        <div
            class="w-full hidden lg:flex lg:w-1/3 p-4 items-center justify-center"
        >
            <div
                class="rounded-2xl overflow-hidden shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 w-full max-w-md"
            >
                <img
                    class="w-full h-auto"
                    src="/smile.webp"
                    alt="Smiling patient"
                    loading="lazy"
                />
            </div>
        </div>

        <!-- Right Column -->
        <div class="w-full lg:w-1/3 flex flex-col gap-6 lg:pb-4 p-4">
            <div
                class="bg-transparent transition-all duration-300 flex gap-4 rounded-xl p-5"
            >
                <img
                    class="w-12 h-12 object-contain"
                    src="/teeth-braces.webp"
                    alt="Orthodontics icon"
                />
                <div class="flex flex-col">
                    <h2 class="text-lg md:text-xl font-semibold text-gray-800">
                        Orthodontics
                    </h2>
                    <p class="font-light text-gray-600 mt-1">
                        Some big paragraph will go here so it is a dummy data
                        for now
                    </p>
                </div>
            </div>

            <div
                class="bg-transparent transition-all duration-300 flex gap-4 rounded-xl p-5"
            >
                <img
                    class="w-12 h-12 object-contain"
                    src="/teeth-braces.webp"
                    alt="Orthodontics icon"
                />
                <div class="flex flex-col">
                    <h2 class="text-lg md:text-xl font-semibold text-gray-800">
                        Orthodontics
                    </h2>
                    <p class="font-light text-gray-600 mt-1">
                        Some big paragraph will go here so it is a dummy data
                        for now
                    </p>
                </div>
            </div>

            <div
                class="bg-transparent transition-all duration-300 flex gap-4 rounded-xl p-5"
            >
                <img
                    class="w-12 h-12 object-contain"
                    src="/teeth-braces.webp"
                    alt="Orthodontics icon"
                />
                <div class="flex flex-col">
                    <h2 class="text-lg md:text-xl font-semibold text-gray-800">
                        Orthodontics
                    </h2>
                    <p class="font-light text-gray-600 mt-1">
                        Some big paragraph will go here so it is a dummy data
                        for now
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
